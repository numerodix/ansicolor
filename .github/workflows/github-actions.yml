name: ansicolor github actions CI
on: [push]
jobs:
  run-unit-tests:
    runs-on: ubuntu-latest
    steps:
      - run: ls
      - run: pwd
      - run: pip3 install -r dev-requirements.txt
      - run: py.test